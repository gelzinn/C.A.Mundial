import { LoadingRing } from "../LoadingCircle/styles";
import { LoadingContainer } from "./styles";

export default function LoadingCircle() {
  return (
    <LoadingContainer>
      <LoadingRing />
    </LoadingContainer>
  );
}
